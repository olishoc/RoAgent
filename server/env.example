# RoAgent daemon environment overrides
# Copy to your process manager environment, not to source control with real secrets.

PLUGIN_PORT=8765
PLUGIN_DATA_DIR=../data
PLUGIN_LOG_LEVEL=info
# Optional. If omitted, the daemon creates a local token in PLUGIN_DATA_DIR/auth-token.
PLUGIN_AUTH_TOKEN=replace-with-local-token
